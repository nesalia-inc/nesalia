---
title: "Dictionaries"
description: "Learn about dictionaries, Python's powerful key-value pair data structure."
order: 4
---

# Dictionaries

We use dictionaries when we need to store and retrieve data using custom keys instead of numeric indices. A dictionary is an unordered collection of key-value pairs, where each key maps to a specific value.

## Creating Dictionaries

We create dictionaries using curly braces with key-value pairs:

```python
# Empty dictionary
empty_dict = {}

# Dictionary with key-value pairs
person = {
    "name": "Alice",
    "age": 25,
    "city": "New York"
}

# Using dict() constructor
person = dict(name="Alice", age=25, city="New York")

# Creating from a list of tuples
pairs = [("name", "Alice"), ("age", 25)]
person = dict(pairs)
```

## Accessing Values

### Using Keys

We access values using bracket notation with the key:

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

print(person["name"])               # Alice
print(person["age"])                # 25

# Using get() method with optional default
print(person.get("name"))                   # Alice
print(person.get("country", "USA"))          # USA (default when key missing)
print(person.get("country"))                # None (no default specified)
```

### Keys vs Indices

Dictionary keys are not numeric indices:

```python
# Attempting numeric indexing raises KeyError
# person[0]  # KeyError!

# We always use the actual key
print(person["name"])  # Alice
```

## Adding and Modifying Entries

```python
person = {"name": "Alice", "age": 25}

# Add new key-value pair
person["city"] = "New York"

# Modify existing value
person["age"] = 26

# Add multiple values
person.update({"country": "USA", "occupation": "Engineer"})

print(person)
# {'name': 'Alice', 'age': 26, 'city': 'New York', 'country': 'USA', 'occupation': 'Engineer'}
```

## Removing Entries

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

# Remove by key (raises KeyError if key missing)
removed = person.pop("city")

# Remove by key with default (does not raise error)
removed = person.pop("country", None)

# Remove and return last inserted pair (Python 3.7+)
last_item = person.popitem()

# Clear all entries
person.clear()

print(person)  # {}
```

## Dictionary Methods

### Getting Information

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

print(len(person))              # 3 (number of key-value pairs)
print(list(person.keys()))      # ['name', 'age', 'city']
print(list(person.values()))    # ['Alice', 25, 'New York']
print(list(person.items()))     # [('name', 'Alice'), ('age', 25), ('city', 'New York')]
```

### Checking Keys

```python
person = {"name": "Alice", "age": 25}

print("name" in person)        # True (checks keys)
print("Alice" in person)       # False (values require different check)
print("country" not in person)  # True
```

## Iterating Over Dictionaries

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

# Iterate over keys (default behavior)
for key in person:
    print(key)

# Iterate over keys explicitly
for key in person.keys():
    print(key)

# Iterate over values
for value in person.values():
    print(value)

# Iterate over key-value pairs
for key, value in person.items():
    print(f"{key}: {value}")
```

### Iterating in Order

Python 3.7+ maintains insertion order:

```python
person = {}
person["name"] = "Alice"
person["age"] = 25
person["city"] = "New York"

for key, value in person.items():
    print(f"{key}: {value}")
# Output (in order):
# name: Alice
# age: 25
# city: New York
```

## Nested Dictionaries

Dictionaries can contain other dictionaries.

### Nested Dictionary Structure

```python
employees = {
    "emp1": {"name": "Alice", "role": "Engineer"},
    "emp2": {"name": "Bob", "role": "Manager"},
    "emp3": {"name": "Charlie", "role": "Designer"}
}

print(employees["emp1"]["name"])  # Alice
print(employees["emp2"]["role"])   # Manager

# Access nested elements safely
role = employees.get("emp4", {}).get("role")
print(role)  # None
```

### Lists of Dictionaries

```python
students = [
    {"name": "Alice", "grade": 85},
    {"name": "Bob", "grade": 92},
    {"name": "Charlie", "grade": 78}
]

for student in students:
    print(f"{student['name']}: {student['grade']}")
```

### Dictionary Containing Lists

```python
courses = {
    "Python": ["Alice", "Bob", "Charlie"],
    "JavaScript": ["David", "Eve"],
    "Java": ["Frank", "Alice"]
}

print(courses["Python"])     # ['Alice', 'Bob', 'Charlie']
courses["Python"].append("David")
```

## Dictionary Comprehensions

```python
# Basic dictionary comprehension
squares = {x: x**2 for x in range(5)}
print(squares)  # {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}

# With condition
even_squares = {x: x**2 for x in range(10) if x % 2 == 0}
print(even_squares)  # {0: 0, 2: 4, 4: 16, 6: 36, 8: 64}

# Transform existing dictionary
original = {"a": 1, "b": 2, "c": 3}
doubled = {k: v*2 for k, v in original.items()}
print(doubled)  # {'a': 2, 'b': 4, 'c': 6}
```

## Useful Patterns

### Default Values with defaultdict

```python
from collections import defaultdict

# Create dictionary with default values for missing keys
counts = defaultdict(int)
words = ["apple", "banana", "apple", "cherry", "banana", "apple"]

for word in words:
    counts[word] += 1

print(counts)  # defaultdict(<class 'int'>, {'apple': 3, 'banana': 2, 'cherry': 1})
```

### Counting with get()

```python
words = ["apple", "banana", "apple", "cherry", "banana", "apple"]
counts = {}

for word in words:
    counts[word] = counts.get(word, 0) + 1

print(counts)  # {'apple': 3, 'banana': 2, 'cherry': 1}
```

### Using setdefault()

```python
# setdefault returns the value if key exists, otherwise sets and returns the default
person = {"name": "Alice", "age": 25}

# Key exists - returns current value
name = person.setdefault("name", "Unknown")  # Returns "Alice"

# Key does not exist - sets to default and returns it
country = person.setdefault("country", "USA")
print(person)  # {'name': 'Alice', 'age': 25, 'country': 'USA'}
```

## Practical Examples

### Example 1: Phonebook

```python
phonebook = {
    "Alice": "555-1234",
    "Bob": "555-5678",
    "Charlie": "555-9012"
}

# Add a new contact
phonebook["David"] = "555-3456"

# Look up a phone number
name = "Alice"
if name in phonebook:
    print(f"{name}: {phonebook[name]}")
else:
    print(f"{name} not found")

# Delete a contact
del phonebook["Bob"]

# Display all contacts
for name, phone in phonebook.items():
    print(f"{name}: {phone}")
```

### Example 2: Word Frequency Counter

```python
text = "the quick brown fox jumps over the lazy dog the fox"

# Count word frequencies
word_counts = {}
for word in text.split():
    word_counts[word] = word_counts.get(word, 0) + 1

print(word_counts)
# {'the': 2, 'quick': 1, 'brown': 1, 'fox': 2, 'jumps': 1,
#  'over': 1, 'lazy': 1, 'dog': 1}
```

### Example 3: Inverting a Dictionary

```python
# Swap keys and values (works when values are unique)
original = {"a": 1, "b": 2, "c": 3}
inverted = {v: k for k, v in original.items()}

print(inverted)  # {1: 'a', 2: 'b', 3: 'c'}

# Preserve all mappings when values are not unique
original = {"a": 1, "b": 2, "c": 1}
from collections import defaultdict
inverted = defaultdict(list)
for k, v in original.items():
    inverted[v].append(k)

print(dict(inverted))  # {1: ['a', 'c'], 2: ['b']}
```

### Example 4: Configuration Settings

```python
# Store and retrieve nested configuration
config = {
    "database": {
        "host": "localhost",
        "port": 5432,
        "name": "mydb"
    },
    "server": {
        "host": "0.0.0.0",
        "port": 8000
    }
}

# Access nested values
db_host = config["database"]["host"]
server_port = config["server"]["port"]

print(f"Database: {db_host}:{config['database']['port']}")
print(f"Server: {server_port}")

# Update configuration safely
config.setdefault("cache", {"host": "localhost", "port": 6379})
```

### Example 5: Student Grades

```python
students = {
    "Alice": {"math": 85, "science": 92, "english": 88},
    "Bob": {"math": 78, "science": 81, "english": 90},
    "Charlie": {"math": 95, "science": 88, "english": 82}
}

# Calculate average for each student
for name, grades in students.items():
    average = sum(grades.values()) / len(grades)
    print(f"{name}: {average:.2f}")

# Find the highest math grade
math_grades = {name: grades["math"] for name, grades in students.items()}
best_math = max(math_grades, key=math_grades.get)
print(f"Best in math: {best_math} ({math_grades[best_math]})")
```

### Example 6: Grouping Data by Category

```python
from collections import defaultdict

# Group fruits by color
fruits = [
    {"name": "apple", "color": "red"},
    {"name": "banana", "color": "yellow"},
    {"name": "cherry", "color": "red"},
    {"name": "lemon", "color": "yellow"},
    {"name": "strawberry", "color": "red"}
]

# Group by color
by_color = defaultdict(list)
for fruit in fruits:
    by_color[fruit["color"]].append(fruit["name"])

print(dict(by_color))
# {'red': ['apple', 'cherry', 'strawberry'],
#  'yellow': ['banana', 'lemon']}

# Count fruits by color
color_counts = {color: len(fruits) for color, fruits in by_color.items()}
print(color_counts)  # {'red': 3, 'yellow': 2}
```

## When to Use Dictionaries

Dictionaries are the right choice when we need to:

- Store data as key-value pairs
- Look up values quickly using custom keys
- Associate related information without numeric indices
- Group related data together
- Build lookup tables and caches
- Count occurrences with meaningful keys

Dictionaries provide a flexible and efficient way to organize and access data in Python, making them essential for nearly every Python program we write.
